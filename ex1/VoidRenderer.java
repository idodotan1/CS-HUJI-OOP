/**
 * The class used for a void renderer
 */
public class VoidRenderer implements Renderer {
    /**
     * A constructor for the void renderer
     */
    public VoidRenderer() {}

    /**
     * Renders the board in a void way(which means do nothing)
     * @param board the board to render
     */
    @Override
    public void renderBoard(Board board) {
    }
}
